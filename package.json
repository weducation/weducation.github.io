{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "rm -rf docs/; nuxt generate && mv .output/public docs",
    "generate:windows": "rmdir /s /q docs && nuxt generate && move .output/public docs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/fonts": "0.11.1",
    "@nuxt/icon": "1.12.0",
    "@nuxtjs/tailwindcss": "6.13.2",
    "nuxt": "^3.16.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
